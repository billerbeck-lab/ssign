# Run outputs, analysis scripts, and figures stay local.
# Only the structural skeleton + README ship.

*.tar.gz
*.tgz

# CX3 validation run outputs (figure + enrichment validation) — kept local, regenerate on CX3.
cx3_t3ss/
cx3_enrichment/

proximity_window/*
fragmentation/*
copies/*
benchmarking/*
analysis/*

# Keep the empty-dir markers visible to git.
!proximity_window/.gitkeep
!fragmentation/.gitkeep
!copies/.gitkeep
!benchmarking/.gitkeep
!analysis/.gitkeep
