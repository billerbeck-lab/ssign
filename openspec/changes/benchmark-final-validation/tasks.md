## 1. Phase A: full-corpus re-verification

- [ ] 1.1 Build the re-verification input: dump all 337 `positives_all.tsv` effectors with their accession, locus, ss_type, gene, organism, family, citation_quote, and primary-reference DOI into one ledger-input TSV.
- [ ] 1.2 Define the strict anti-hallucination output schema (echoed-identifier-only; verdict ∈ confirmed/fix/drop; provenance quote required) and the per-effector agent prompt, reusing the tasks 74-84 method.
- [ ] 1.3 Run the parallel blind-agent re-verification over all 337 effectors (batched), capturing per-agent verdicts + provenance; resolve content-filter-tripped toxin rows deterministically via UniProt/NCBI/PubMed.
- [ ] 1.4 Verify every primary-reference DOI resolves AND supports the secretion claim; flag citation defects, find replacement references where possible.
- [ ] 1.5 Manual reconciliation pass: adjudicate agent disagreements and unconfirmed claims into a single verdict per effector; write the final verification ledger (337 rows, no blanks).
- [ ] 1.6 Emit the corrections table (drops + fixes) in the scripts 55/56 format and wire it into `clean_dataset` reversibly (new `removed_*`/corrections file, no figure-script hard-coding).
- [ ] 1.7 Regenerate recall (01-06) + annotation (07/08) figures from the updated `clean_dataset`; record the before/after count delta in NOTES.

## 2. Phase B: build the corrected genome panel

- [x] 2.1 Confirm which T5SS genomes are already staged in `inputs_gb/`; fetch any absent ones (top open question RESOLVED, `scripts/57_build_rerun_panel.py`): 13 of 20 T5SS genomes already in 10 staged units; 7 are cache-not-staged (stage from `refseq_cache`, **0 need a network fetch**). Accession drift (RefSeq NC_ vs INSDC) resolved via the Datasets API + eutils `assemblyacc`.
- [x] 2.2 Derive the enriched panel manifest (`data/phase2/rerun_panel_manifest.tsv`): **59 run-units = 57 distinct assemblies** (50 staged + 7 stage-from-cache), **15** dropped, NOT the 66/16 estimated. Per-genome columns added: NCBI-authoritative organism, `genome_group` (biosample + accession-alias union), systems, n_secreted_proteins, n_found_by_ssign, and the secreted-protein list (`*` = emitted by ssign). Findings: `AE002098` (N. meningitidis MC58 = `NC_003112.2`) rescued from drop; 2 same-assembly merges (PAO1 `AE004091`+`NC_002516.2`; A. fabrum C58 `NC_003063.2`+`NC_003065.3`); corpus organism mislabels corrected (`NC_006351`=B. pseudomallei not B. thailandensis, `NC_013716`=Citrobacter not EPEC, `NC_007508`=Xanthomonas not P. aeruginosa).
- [x] 2.2b Stage rerun inputs (`scripts/58_stage_rerun_inputs.py` -> `data/phase2/rerun_inputs.txt`, 57 inputs): 49 reused (already on CX3) + 8 NEW (7 cached T5SS staged, incl. P. mirabilis 2-replicon merge; + the C58 2-replicon merge). PAO1 collapsed to one copy (`NC_002516.2`; the two units are the same chromosome). All 8 new `.gbff` validated parseable (merged ones have 2 records).
- [x] 2.3 Rerun batches via the ESTABLISHED benchmark system (NOT a custom driver, that was a mis-step). Added a `REANNOTATE=1` knob to `run_benchmark_batch.pbs` (genbank input + Bakta re-annotation = clean slate, no `--use-input-annotations`). `make_rerun_batches.py` splits the 57 into 12 batch files (`rerun_batches/rerun_NN.txt`, 4-5 genomes each) + writes `SUBMIT_rerun.sh` that qsubs all 12 at once (PBS schedules up to the concurrent-job cap; no trickling). Config per batch: `INPUT_MODE=genbank,REANNOTATE=1,INCLUDE_T3SS=1,WHOLE_GENOME=1,ENRICH=1,ANNOT=1`, 32c/64gb/RTX6000, 72h walltime, `RUN_TAG=rerun`.
- [ ] 2.4 (Teo, on CX3) `git pull` (gets PBS + batches + SUBMIT; gbff already rsynced) -> `bash validation_sweeps/benchmark/scripts/cx3/SUBMIT_rerun.sh`. Results land in `~/runs/benchmark_phase2/rerun/<unit>/`.

## 3. Phase B: run on CX3 (user-driven)

- [ ] 3.1 On CX3: `git pull` the branch, run `ssign doctor --tier extended`, re-fetch any purged `$EPHEMERAL` databases.
- [ ] 3.2 Submit the 66-genome panel in overnight batches via `cx3-submit`; monitor each batch to `Pipeline complete` / per-genome `N/N steps succeeded`.
- [ ] 3.3 Retrieve per-genome + pooled enrichment TSVs, figures, `combined_results.csv`, and run logs to the laptop.

## 4. Phase B: reconcile + close

- [x] 4.1 Confirm the four RTX T1SS toxins (HlyA, ApxIA, LtxA, LktA) emit as secreted from their real full-assembly runs; mark the `clean_dataset` staging fix retired (flag stays as fallback). **DONE.** The targeted `rerun_fullasm/` CX3 job landed; coordinate-joining the toxins to it confirms **hlyA (NJDLCG_05342), apxIA (KKEJGA_00582), ltxA (BKOJAN_00117) all emit** (DLP+DSE, DLP 0.87-0.98, nearby T1SS). lktA P55117 (`NZ_SMAM01000003.1`) was not in the fullasm rerun, but the other two lktA instances (P0C082, Q9ETX2) already emit from complete genomes, so every RTX toxin type emits on a complete assembly. **P55117 was NOT stale** (it is lktA in a real draft genome, distinct from P0C082/Q9ETX2). `APPLY_T1SS_STAGING_FIX` left OFF and documented as retired/superseded (`clean_dataset.py` docstring updated): the real emission goes into the figures via 4.4, not the manual flag. Serralysin (P23694) is predicted secreted (DLP 0.9992) but NOT emitted even from the full assembly (no T1SS machinery within +/-3) = genuine proximity miss, a 4.4 datapoint.
- [x] 4.2 Grade annotation correctness for the T5SS effectors from the rerun; extend `annotation_correctness.py` and regenerate 07/08. **DONE.** Added a reusable coordinate-join helper (`scripts/rerun_coords.py`: `RerunIndex`, max-overlap on (contig,start,stop) since Bakta renames loci) and rewired `annotation_correctness.py` to source T5SS from `rerun/` (was a dead `/tmp/ssign_fleet_67` path). Corpus has 23 T5SS effectors, **13 emit in the rerun and are graded** (not the 15 "found" estimated by `53_t5ss_recall`, see below); the other 8 don't emit (no annotation), 2 (prn, fhaB) aren't in the panel. Re-sourcing reproduces the old verdicts for the 4 overlapping effectors (espP/pic/yadA/cdrA) = coordinate-join validated. **All 13 graded overall-correct**; per-tool the only T5SS misses are Pfam on the two TAAs (yadA wrong DUF814/FbpA, nadA partial). Pfam channel switched to `eggnog__pfam_ids` (rerun Bakta emits no `pfam_ids`; EggNOG gives the same short-name format the fleet used). Sheet now 55 rows (42 proximity + 13 T5SS); figs 07 = 45/54 correct (83%), 08 = T5SS 13/13. Verdicts are gene-keyed (`T5SS_V`) so they survive Bakta locus drift.
- [x] 4.3 Verify the per-SS-type circular-shift enrichment emits sane fold/p/q across the panel (including a T3SS DLP row, no T3SS DSE row); close task 70. **DONE:** fleet-wide scan of all 57 `*_enrichment_stats.tsv` = 444 rows, 0 numeric issues (every p_perm/qvalue in [0,1], every fold finite). Bug-fix #4 holds in every genome: all 21 T3SS DSE rows blanked (no observed), all 21 T3SS DLP rows populated (7 significant). Pooled (`rerun/_pooled/pooled_enrichment_stats.tsv`): T3SS DLP fold 5.90 q=2e-4 sig, NO T3SS DSE row; T1/T2/T3/T5a/T5b/T6 significant (DLP+DSE), T4SS+T5cSS n.s. (low count). (close task 70 = docs in 4.5.)
- [~] 4.4 Reconcile rerun recall against figure 06 (does the real run match the strict-emission counts?); document any discrepancies in NOTES. **RECONCILED, figure rebuild HELD (Teo 2026-06-25): hold until Phase A re-audit, which will change the effector set anyway.** Coordinate-joined all 496 testable effectors (`rerun_coords` + gbff coord parse) to the tier-2 rerun: **471/481 joined agree (98%)**. Disagreements: **+3** (hlyA/apxIA/ltxA now emit, the fragment-bug fix), **-6 in-figure** (rerun does NOT emit RipJ, NleB [T3SS]; Tae4_Stm, Tle1_Sci1, BopE, TseM [T6SS] -- clean-slate Bakta shifts these borderline calls; RipJ/Tae4/Tle1 are exact 1.00-overlap non-emissions, e.g. RipJ's locus reannotated as machinery "HrpB7"), plus BopA (already audit-filtered). **15 unjoinable** = genomes outside the 57-panel (+ a couple contig-alias gaps like aprA `AE004091.2`->`NC_002516.2`). When Phase A is done and the effector set is final, rebuild ssign_call from the rerun (prefer `rerun_fullasm/` for the 4) and regen figs 01-06; decide then whether to adopt the rerun fully (+3/-6) or patch toxins only, and resolve the default-vs-T3SS tag (rerun is T3SS-on only).
  - **4.4a Geometry + overlap found_by_ssign audit over the 90-row gold list — DONE 2026-06-29 (front-half of the recall rebuild).** `scripts/71_geometry_overlap_audit.py` recomputes BOTH recall-bearing columns for all 90 rows and diffs vs stored. (a) Geometry reuses `Reanchor.geometry()` verbatim: **0 true mis-anchors** among the 58 machinery-resolved rows (58 match, 21 `no_machinery_instance` = RTX panel + T5SS self-secretors absent from `machinery_resolved.tsv`, 11 effector-locus `unindexed`). (b) found_by_ssign re-scored by the agreed rule (Teo 2026-06-29): **any bp overlap with an EMITTED secreted protein on the molecule = found**; ORF-called-but-not-emitted or Bakta-gene-miss = genuine fail. New `RerunIndex.emitted_overlap()` is the canonical matcher; `scripts/65` now recomputes found_by_ssign for every row from its coords (no longer goes stale). Molecule identity reconciled first: 3 RefSeq↔INSDC contig aliases verified at lenratio 1.000 (`AE004091.2↔NC_002516.2` PAO1, `NC_002929.2↔BX470248.1` B. pertussis, + MC58); **all 90 reconcile, 0 un-reconcilable**. **6 found corrections** (all 3 matchers — any-overlap, max-overlap, 3'-stop — agree on each): apxIA/hlyA/ltxA no→yes (rerun_fullasm emits them, stored was stale), TseM/Tae4/Tle1 yes→no (ORF annotated but ssign never emitted it). **Recall under the rule: 37/90 = 41%** at audit time; per-type T1 16/18, T2 1/8, T3 6/23, T4 0/8, T5 10/19, T6 4/14. T4SS 0% is the correct consequence of the cross-replicon re-anchors (effectors distal from VirB), not a bug. Output: `geometry_overlap_audit.tsv`.
  - **4.4b High-churn re-review + geometry recompute — DONE 2026-06-29 pm.** Re-reviewed the 30 rows with >=2 correction types (blind 3-agent, scripts/72 deterministic length/overlap check, `gold_review2/sweep3_highchurn/reconciliation.tsv`). **Found + fixed 2 wrong-gene errors** (an earlier swap_up anchored them onto a structural/regulatory paralog, confirmed by Pfam): T6SS_18 Tle1 (old D3GUV9=OmpA/TagL -> B7LG63/EC042_RS24220, real Tle1-EAEC PF09994) and T6SS_17 Tae4 (old A0A0F6AX88=TagF PF09867 -> A0A0F6AX79/STM14_RS01975, real Tae4 amidase). Both real effectors EMIT in the rerun -> **found no->yes, recall 37->39/90 = 43% (no-T5SS 29/71); T6SS 4->6/14.** Other re-review flags (5 fix_quote, 1 weak) were pre-adjudicated `note`-kept; T5SS_02 Ag43 quote flagged to Teo. **Geometry recomputed (scripts/73)** for 34 rows from rerun-DETECTED components (not the partial machinery_resolved): RTX-T1SS distances confirmed, T5bSS "(self-secreting)" mislabel -> real TpsB distance (8/10 reachable), self-secretors honestly relabeled, T6SS_17/18 d1/reachable. Geometry does NOT change found/recall.
  - **4.4c All-90 reachable recompute — DONE 2026-06-29 eve.** Generalized scripts/73 to recompute nearest/distance/reachable for ALL 90 rows from rerun-DETECTED cognate machinery, retiring `machinery_resolved.tsv` for these columns (it was a partial list placing effectors 100-3000 genes from machinery ssign emitted via proximity). Fixed a real bug (the `(other)` secretion-system subsection was being dropped). Clean found×reachable 2x2: found=yes/reach=yes 31, /self 7, /no 1 (VirA, recovered via a non-cognate T5aSS neighbor); found=no/reach=yes 9 (gate-failure misses), /no 40, /self 2. Miss categories for the figure captured in `geometry_recompute.tsv` status col (reachable-but-missed / far-from-machinery / machinery-not-detected-or-cross-replicon / self-secretor / Bakta-gene-miss). recall unchanged 39/90. **Remaining half of 4.4 = wire the 39/90 recall + miss categories into figs 01-06.**
- [ ] 4.5 Update the benchmark docs/methods with the re-verified answer key and the tier-2 rerun provenance; mark tasks 70 + 71 done.
