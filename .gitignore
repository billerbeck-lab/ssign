# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
dist/
build/
*.whl

# Stale / duplicate directories
bin/bin/
app/
gui/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# ssign runtime
ssign_results/
ssign_progress.json
*_progress.json
*.tmp
.ssign/

# Nextflow
.nextflow/
.nextflow.log*
work/

# Test artifacts
tests/e2e_work/
.pytest_cache/
htmlcov/
.coverage

# Large model / data files — data/ and models/ are fetched at install time
# (databases) or auto-downloaded/baked (weights), never committed. Their reference
# content now lives in docs/how-to/install.md (#32); the dirs are fully local.
*.pt
*.bin
*.safetensors
*.h5
*.hdf5
*.npy
*.npz
*.parquet
data/
models/

# Container artifacts (built locally, not committed)
*.sif
*.tar
*.tar.gz

# Secrets / credentials
.env
*.pem

# Claude Code instructions (not for public release)
CLAUDE.md
.claude/

# Internal planning + scratch — kept locally, not in the public repo (#32 repo TLC)
openspec/
NOTES.md

# Empty placeholder dirs removed from git until they hold real content (#32).
results/
figures/
colab/

# Internal dev scaffolding + point-in-time audits — kept local, not public (#32)
docs/development/
docs/research_software_audit.md

# scripts/ slimmed to user-facing helpers — dev/ops scripts kept local (#32).
# cx3/ = Imperial-cluster ops; the .py = dev tools (monitor_resources superseded
# by core/resource_sampler.py; audit_* = size measurement; chop_genome/analyse_k12
# = one-off QA). Their unit tests go with them.
scripts/cx3/
scripts/chop_genome.py
scripts/audit_database_sizes.py
scripts/audit_disk_sizes.py
scripts/analyse_k12_runs.py
scripts/monitor_resources.py
tests/unit/test_audit_database_sizes.py
tests/unit/test_audit_disk_sizes.py
tests/unit/test_analyse_k12_runs.py

# Root files kept local (#32): SYSADMIN = skeleton hosted-service runbook (service
# not built); requirements-baseline = stale (old repo pin + removed pybiolib,
# redundant w/ pyproject+uv.lock); env_signalp6.lock = pins a /home/teo file:// path;
# .dockerignore = dead (no Dockerfile, build is Apptainer).
SYSADMIN.md
requirements-baseline.txt
env_signalp6.lock.txt
.dockerignore

# Local drafts (correspondence, WIP notes not intended for commit)
.drafts/

# Stray pipeline outputs from interactive testing
macsyfinder_out/

# Benchmark + validation sweeps — kept local, not in the public repo (#32).
# Mid-flight; curated deliverables get re-added to a clean path when it closes.
validation_sweeps/
